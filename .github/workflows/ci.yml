name: CI — Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    name: Run backend tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        working-directory: backend
        run: |
          uv sync --dev

      - name: Run tests with coverage
        working-directory: backend
        env:
          # Fake keys so tests don't need real credentials
          SECRET_KEY: "ci-test-secret-key-not-for-production"
          LLM_API_KEY: "sk-fake-key-for-ci"
          ZEP_API_KEY: "fake-zep-key-for-ci"
          FLASK_DEBUG: "True"
          ALLOWED_ORIGINS: "http://localhost:3000"
        run: |
          uv run pytest tests/ -v --tb=short --no-header \
            --cov=app \
            --cov-report=term-missing \
            --cov-report=xml:coverage.xml \
            --cov-report=html:htmlcov \
            --cov-fail-under=30

      - name: Upload HTML coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: backend/htmlcov/
          retention-days: 30

      - name: Upload coverage XML (for Codecov)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: backend/coverage.xml
          retention-days: 14

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: backend/coverage.xml
          flags: backend
          name: mirofish-backend
          fail_ci_if_error: false   # don't break CI if Codecov is down
